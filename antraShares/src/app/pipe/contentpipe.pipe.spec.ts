import { ContentpipePipe } from './contentpipe.pipe';

describe('ContentpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentpipePipe();
    expect(pipe).toBeTruthy();
  });
});
