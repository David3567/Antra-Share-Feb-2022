import { ZipcodeDirective } from './zipcode.directive';

describe('ZipcodeDirective', () => {
  it('should create an instance', () => {
    const directive = new ZipcodeDirective();
    expect(directive).toBeTruthy();
  });
});
